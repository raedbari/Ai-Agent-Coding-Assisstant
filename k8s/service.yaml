apiVersion: v1
kind: Service
metadata:
  name: ai-coding-assistant-service
  namespace: ai-coding
spec:
  type: NodePort
  selector:
    app: ai-coding-assistant
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080